# Handoff — Sesión 2026-06-23

## Resumen de la sesión

Sesión de documentación y consolidación arquitectónica. No se modificó código fuente. Todo el trabajo fue:

1. **Generación de documentación técnica** (`docs/`) a partir del grafo de Graphify
2. **Registro de reglas arquitectónicas** en memoria persistente
3. **Actualización del roadmap** con la estructura de versiones V1 / V1.1 / V2
4. **Creación de ADRs** para decisiones ya tomadas

---

## Artefactos creados o modificados

### Documentación técnica — `docs/`
Generados por agente a partir de `graphify-out/GRAPH_REPORT.md` + lectura directa de fuentes:

| Archivo | Contenido |
|---|---|
| `docs/architecture.md` | Stack, rutas, auth flow, offline-first, PWA/Workbox, diagrama ASCII |
| `docs/frontend.md` | Páginas, 23 componentes, sistema de tema, hooks |
| `docs/backend-n8n.md` | 6 endpoints con request/response shapes exactos |
| `docs/database.md` | Esquemas `wines` (21 cols) y `tastings` (11 cols), algoritmo wine_uid |
| `docs/supabase.md` | Setup cliente, auth, RLS, bucket `wine-labels` |
| `docs/storage.md` | localStorage wrapper, IDB (3 object stores), sync queue, Workbox |
| `docs/scan-flow.md` | Flujo completo dos fases, máquina de estados, algoritmo wine_uid paso a paso |
| `docs/roadmap.md` | Versiones V1 ✅ / V1.1 pendiente / V2 planificada |

### ADRs nuevos — `docs/adr/`
- `0003-wine-uid-como-identificador-canonico.md` — SHA-256 de nombre+bodega+añada, algoritmo idéntico en frontend y n8n
- `0004-gpt-solo-si-vino-no-existe.md` — GPT solo en Fase 2, solo si `found = false`
- `0005-qr-primera-estrategia-identificacion.md` — QR primero, luego wine_uid, luego GPT (QR pendiente de implementar en V1.1)

### Memoria persistente
- `project_architecture_rules.md` añadida al índice de memoria del proyecto

---

## Estado del proyecto

### V1 — Completada ✅
OCR, GPT, Storage, wine_uid. Ver `docs/roadmap.md`.

### V1.1 — Pendiente
Tres ítems principales:
1. **Identificación rápida** — workflow `Vinoteca – Scan Identificar` ya creado en n8n (ID: `yeSTiVxJSevj2Sbp`), pendiente de configurar credenciales manualmente
2. **QR cache** — lector de QR en cliente antes de enviar imagen a n8n (ADR 0005)
3. **Caché inteligente** — sin especificar aún

### TODOs técnicos pendientes (de V1)
- Migrar `TastingChat` de llamada directa a OpenAI → n8n webhook
- Backfill `wine_uid` para vinos con `wine_uid = NULL` (script en `src/lib/wineDuplicates.ts`)
- Resolver `processOperation` duplicado (`useSync.ts` y `main.tsx`)
- Consolidar `ChatBubble` duplicado (`src/components/wine/` vs `src/components/ui/`)
- Completar offline sync automático al reconectar

---

## Pendiente manual (requiere acción fuera del código)

El workflow `Vinoteca – Scan Identificar` en n8n necesita configuración manual:

1. **Credencial "Header Auth account"** (ID: `mSeQOoORfe8qLqNH`) en n8n UI:
   - Name: `Authorization`
   - Value: `Bearer sk-...` (OpenAI API key)

2. **Variables de entorno en Portainer** (contenedor n8n):
   - `SUPABASE_URL=https://tu-proyecto.supabase.co`
   - `SUPABASE_SERVICE_KEY=<service_role_key>`

---

## Reglas arquitectónicas (invariantes)

Ver `docs/adr/` y memoria `project_architecture_rules.md`. Resumen:
- **n8n** = toda la lógica de negocio. React solo consume APIs. Nunca duplicar.
- **Supabase** = fuente de verdad. IDB = solo caché offline.
- **GPT** = solo si el vino no existe (ADR 0004).
- **QR** = primera estrategia de identificación (ADR 0005, pendiente de implementar).
- **wine_uid** = algoritmo idéntico en ambas capas (ADR 0003).

---

## Suggested skills

- `/brainstorming` — para especificar V1.1 (caché inteligente) o V2 (recomendaciones, compartir bodega) antes de implementar
- `/n8n-mcp-tools-expert` — para configurar o modificar workflows n8n
- `/graphify` — el grafo ya existe en `graphify-out/`; usar `graphify query "<pregunta>"` en vez de leer ficheros

---

## Próximos pasos sugeridos

1. Configurar credenciales del workflow `Scan Identificar` en n8n UI (manual)
2. Integrar `callScanIdentificar` en `Scan.tsx` para activar la Fase 1
3. Especificar "caché inteligente" de V1.1 antes de implementar
