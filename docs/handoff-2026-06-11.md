# Handoff: Vinoteca — Sesión 2026-06-11

## Proyecto
- **Repo:** `/Users/carlosrabadan/Antigravity/Vinoteca`
- **Stack:** Vite + React 19 + TypeScript, Supabase, React Router v7, vite-plugin-pwa
- **n8n instance:** `https://n8n.rabadanhouse.space`
- **Comandos:** `npm run dev -- --host` (expone red local), `npx tsc --noEmit`

---

## Commits de sesiones anteriores

Ver handoff anterior: `docs/handoff-2026-06-10.md`

---

## Cambios de esta sesión (sin commitear aún)

| Archivo | Cambio |
|---|---|
| `src/types/index.ts` | Añadidos 7 campos nuevos a `Wine`: `alcohol`, `crianza`, `descripcion`, `url_bodega`, `temp_servicio`, `contiene`, `volumen` |
| `src/lib/n8n.ts` | `ScanResult` ampliado con 7 campos + `imagen_trasera_url`; timeout 60s; log diagnóstico temporal |
| `src/pages/Scan.tsx` | Estados `studioUrl` + `traseraUrl`; usa imagen studio de n8n como frontal y trasera recortada como trasera |
| `src/pages/WineDetail.tsx` | Ficha ampliada: crianza, alcohol, volumen, temp. servicio, contiene; descripción en cursiva; enlace url_bodega; fix menú ··· iOS; sin "Vintage Reserve" |
| `src/pages/Bodega.tsx` | Botón logout; sin "Colección curada" |
| `src/components/wine/WineCard.tsx` | `object-contain` para botella entera |
| `src/hooks/useCamera.ts` | Logs temporales de diagnóstico |

---

## Migración Supabase aplicada

Ejecutada manualmente en `https://supabase.com/dashboard/project/xagsblgwvfitqkzjtwyc/sql`:
```sql
ALTER TABLE wines
  ADD COLUMN IF NOT EXISTS alcohol       text NULL,
  ADD COLUMN IF NOT EXISTS crianza       text NULL,
  ADD COLUMN IF NOT EXISTS descripcion   text NULL,
  ADD COLUMN IF NOT EXISTS url_bodega    text NULL,
  ADD COLUMN IF NOT EXISTS temp_servicio text NULL,
  ADD COLUMN IF NOT EXISTS contiene      text NULL,
  ADD COLUMN IF NOT EXISTS volumen       text NULL;
```

---

## Estado del workflow n8n (`EtTezN27e9tqvOjS`)

**Activo** — `activeVersionId: 5d0c4836-654f-41f4-ac21-a7b8c64c766d`

### Flujo
```
Recibir Imagen
    ├→ Preparar Mensajes → Analizar Vision → Parsear Resultado → [IF Tiene Nombre]
    │                                                            ↙ TRUE    FALSE ↘
    │                                                   Generar Studio    Buscar Fallback
    │                                                            ↘              ↙
    │                                                        Merge Resultados (Code)
    │                                                                ↑
    └→ Preparar Trasera → [IF Tiene Trasera]
                              ↙ TRUE      FALSE ↘
                        Recortar Trasera    Sin Trasera
                              ↘                ↙
                           Merge Resultados (Code)
                                    ↓
                          Extraer URL Imagen → Responder
```

### Variables de entorno en n8n (ya configuradas en Portainer)
- `OPENAI_API_KEY` — para "Recortar Trasera" vía `$env`
- `NODE_FUNCTION_ALLOW_BUILTIN=*` — para `require('https')` en Code nodes

### Nodos destacados
- **Generar Imagen Studio**: `gpt-image-1`, `1024x1536`, `quality: medium`, iluminación fuerte izquierda, fondo marrón cálido
- **Recortar Trasera**: Code node, `POST /v1/images/edits` multipart, extrae etiqueta trasera sobre fondo transparente (PNG)
- **Extraer URL Imagen**: devuelve `imagen_url` + `imagen_trasera_url` al cliente

### Advertencias de validación (falsos positivos, no bloquean)
- `INVALID_PARAMETER` en "Generar Imagen Studio" y "Analizar Vision"
- `HARDCODED_CREDENTIALS` en "Buscar Imagen Fallback" (SerpAPI key en claro)

---

## Pendientes

1. **Probar escaneo real con trasera** — verificar que etiqueta trasera llega recortada y limpia
2. **WineForm** — comprobar/añadir campos nuevos (alcohol, crianza, descripcion, url_bodega, temp_servicio, contiene, volumen) al formulario de edición
3. **Limpiar logs temporales** — `useCamera.ts` y `n8n.ts`
4. **Commit** — 7 archivos sin commitear

---

## Archivos clave

- `src/lib/uuid.ts` — polyfill `randomUUID()` para Safari iOS
- `src/lib/n8n.ts` — `callScanAnalizar()`, `ScanResult`, timeout 60s
- `src/lib/storage.ts` — `uploadWineImage()`, `fetchImageAsDataUrl()`
- `src/hooks/useCamera.ts` — captura y compresión, logs temporales
- `src/hooks/useWines.ts` — `createWine()` con upload resiliente
- `src/pages/Scan.tsx` — flujo 4 pasos, `studioUrl` + `traseraUrl`
- `src/pages/WineDetail.tsx` — ficha completa con datos trasera
- `src/components/wine/WineCard.tsx` — `object-contain`
- `src/pages/Bodega.tsx` — logout, filtros

---

## Contexto de referencia

- Handoff anterior: `docs/handoff-2026-06-10.md`
- ADR scan workflow: `docs/adr/0002-scan-workflow-via-n8n.md`
- Memoria: `/Users/carlosrabadan/.claude/projects/-Users-carlosrabadan-Antigravity-Vinoteca/memory/`

---

## Suggested skills

- `verification-before-completion` — probar scan real con trasera
- `n8n-mcp-tools-expert` — ajustar nodo "Recortar Trasera" si hay errores con imagen real
